[project]
name = "genai-pipeline-project"
version = "0.1.0"

[tool.fastapi]
entrypoint = "services.fastapi-app.src.fastapi-app.main:app"

[tool.uv.workspace]
members = [
    "packages/feedpoller",
    "services/fastapi-app",
]
